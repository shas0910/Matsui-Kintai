<h2>月一覧</h2>

<ul>
  <% @year_months.each do |ym| %>
    <li><%= link_to "#{ym.year}年#{ym.month}月", edit_year_month_path(ym.id) %></li>
  <% end %>
</ul>
<%= link_to "月度カレンダー新規作成", new_year_month_path %>