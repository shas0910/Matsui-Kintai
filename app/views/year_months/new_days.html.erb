<h2>カレンダー登録</h2>

<%= form_for @day do |f| %>
  
  <% (Date.parse(session["year_month"][:year_month]["first_date"].to_s)..Date.parse(session["year_month"][:year_month]["last_date"].to_s)).each do |date| %>
    <% days = ["日", "月", "火", "水", "木", "金", "土"] %>
    <div>
      <%= f.select :date, [[date, date]] %>
      <span>(<%= days[date.wday] %>)</span>
    </div>
  <% end %>

<% end %>